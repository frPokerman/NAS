search:
    # @name Display search bar
    # @desc If a search bar is displayed in the file explorer.
    search-bar: true
    # @desc If the search bar can search in files content.
    content-search: true